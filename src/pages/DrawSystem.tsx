import { Typography, Card } from 'antd'

const { Title } = Typography

const DrawSystem = () => {
  return (
    <div>
      <Title level={2}>抽签工作台</Title>
      <Card>
        <div style={{ padding: '20px', textAlign: 'center' }}>
          该功能已被移除
        </div>
      </Card>
    </div>
  )
}

export default DrawSystem 